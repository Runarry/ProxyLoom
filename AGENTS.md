# ProxyLoom 开发约定

## 项目与依据

- 自托管的 Xray、sing-box、Mihomo 订阅管理与测试平台；P0 为单工作空间、可信管理员。
- 需求与验收以 `docs/requirements_v1.0.md` 为准，架构以 `docs/project_design_v1.0.md` 为准，实施顺序按 `docs/PLAN.md`。冲突先记录并协调，不擅自缩减范围。
- 沿用现有目录：`proxyloom-server/` 为 Go API，`proxyloom-runner/` 为独立 Go 执行器，`proxyloom-web/` 为 Vue 3 + TypeScript + Vite 前端。文档中的 `web/`、`cmd/` 为规划布局，落地时结合实际目录。
- 使用 PostgreSQL、Docker Compose；新代码命名统一为 `proxyloom` / `PROXYLOOM_*`，保留历史文档编号。

## 实现约束

- 使用类型化 IR 与独立内核适配器；引用使用稳定 ID，修订不可变。编译只依赖冻结输入，输出字节确定，不读取网络或当前时间。
- P0 链仅含两个具体节点，末跳为出口，不修改原 Node；不改变远端协议，不静默丢字段、降级或在失败时直连。
- 所有启用目标通过真实内核校验后原子发布。订阅 GET 只读发布快照，实时检查授权与安全撤销，数据库不可用时拒绝返回配置。
- API 不运行内核；Runner 不持有数据库凭证或全局主密钥，不接受任意 shell、路径或用户原生配置；网络测试遵守隔离与资源预算。
- 不提交或记录真实节点凭证、订阅令牌、密钥及含秘密配置；示例和测试证据必须脱敏。
- 锁定依赖、内核版本与构建摘要；迁移只追加，不改已执行迁移。API、Schema 或行为变更同步相关契约、文档和夹具。

## 验证

- 按改动运行适用的格式检查、静态分析和测试；核心逻辑及内核适配必须覆盖相关正负例与真实执行。低影响、可逆改动不新增仅复述实现的测试。
- 命令以实际工程脚本为准；文档中的 `make test-*` 尚为规划。未执行或受阻须如实说明，不能声称通过或标记 `verified`；可追溯证据保存到 `docs/evidence/T-xxx/` 或受控 CI 附件。
