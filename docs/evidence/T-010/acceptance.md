# T-010 持久导入验收

源码基点及统一验证摘要沿用 `../T-009/m1-validation/`。新增迁移 000006，原有迁移不变；创建批次、加密原文和 import_parse 任务同事务提交，候选加密保存，预览不写正式节点。

真实 PostgreSQL 四项导入用例全部通过，完整名称和输出见 `../T-009/m1-validation/postgres-report.json` 与 `postgres-tests.jsonl`：

- 5,000 条跨页选择、并发确认胜者、全部写入和相同幂等请求回执。
- 冲突整批回滚、重复内容仅提示、非法候选不能提交。
- 租约 fencing、过期状态、任务与批次原子创建、有效租约保护及恢复。
- update 修订与作用域约束、签名游标、审计原子性、回执保留及用户锁顺序回归。

HTTP 测试另覆盖 multipart／请求边界和 5,000 决策容量。原始输入与未确认候选七天清理；有效任务和正在提交事务避让。HMAC 使用独立用途且排除名字、资源 ID、修订；source_id／bind 尚未支持时明确拒绝。

真实浏览器另完成 205 条跨页确认、混合诊断、文件 Base64、重新载入与过期显示，见 `../T-048/independent-acceptance.md`。浏览器没有执行 5,000 条提交，其容量证据来自上述真实后端测试；没有将两者混为同一次 E2E。
