# T-043 M3 预算分项证据

`m3-budget/report.json` 与源清单记录真实 PostgreSQL 17.6 验证：并发额度不超卖、整批回滚、重复结果不扣两次、attempt 独立预留、跨 UTC 日结算、排队取消释放、失联下载不重试、本机和全局槽位。另覆盖幂等重放不访问新 DNS、离线校验、历史修订及稳定分页。13 个适用实库测试通过，无跳过，隔离容器和网络清理通过。

这是分项验收，不能替代 M3 故障矩阵、部署网络隔离或 G3 总验收。人日实际投入未采集，不把工具运行时长换算成人日。
