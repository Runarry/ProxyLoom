# T-051 M3 浏览器分项证据

`m3-browser/report.json` 及 `browser.txt` 记录 3 个真实 Edge 验收全部通过。浏览器连接真实 API、PostgreSQL、mTLS Runner 和锁定内核，不使用替代 API 响应：

- 节点及两跳批量测试、刷新恢复、完整依赖修订过期、390px 移动查看与键盘操作。
- 下载期间断网，验证重新发起 SSE 请求；先恢复快照、再续接事件；取消后终态持久，刷新不重复下载。
- 中文 HTTP 目标登记与受保护地址拒绝。

前端类型检查和生产构建通过。概览、内核和系统页面仍属后续工作，此处不关闭 T-051 父任务或 G3。
