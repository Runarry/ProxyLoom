# T-058 容量与兼容回归

[原生配置矩阵](native-compatibility/native-config-matrix.json)包含 72 个锁定内核配置正反例：Xray 24、sing-box 23、Mihomo 25。策略、DNS、路由和已批准的限制保持原范围。[真实 Trojan 两跳](live-chain/live-chain-report.json)的 24 个场景全部通过，覆盖方向、复用、故障、清理及观测绕行反例。均为 Linux/amd64，不据此升级 arm64 或未验证客户端。

[覆盖率](coverage/core.out)为 importparse、IR、compiler、三个适配器的合并语句覆盖 81.8%；独立正确性由正反例和真实内核执行补充。该次覆盖测量使用 Go 1.26.0；后续 Go 1.26.8 的完整适用功能与 race 回归另外记录。

[容量历史](preflights/index.json)保留失败与短时预检。原 4 连接池保持不变，修复了订阅列表重复事务、读取资源重复规范化和大型密文信封重复解码；API 配额由 1 调整为 2 CPU。数据仍为 10,000 节点 / 500 链 / 200 订阅，管理分页 100 条、并发 10；订阅产物 1,249,001 字节、并发 50，阈值没有降低。

最后短时预检 P95 为管理 302 ms、订阅 TTFB 416 ms，零错误，规模恢复 28 秒。正式运行要求每内核 100 次编译、20 分钟管理负载和 120 秒订阅负载；短时结果不能替代正式基线。环境与测量方式见 [容量契约](../../m3-capacity-contract.md)。

## 正式运行通过

[正式报告](baseline/report.json)的运行 `4b8ef367-283a-4755-ac33-575219840b38`已通过，`baseline_run=true`，测试资源清理成功。实测宿主配额 4 CPU / 6 GiB，外层 Docker 可用 16 CPU / 约 39.1 GiB；三生产服务配额与脚本摘要均记录在报告中。

| 测量 | 实测 | 结论 |
| --- | --- | --- |
| 管理 10 并发 | 1,200.168 秒，52,635 请求，0 错误，P95 311.013 ms | 通过 ≤500 ms |
| 订阅 50 并发 | 120.295 秒，16,546 请求，0 错误，1,249,001 字节，TTFB P95 486.363 ms | 通过 ≤500 ms |
| Xray / sing-box / Mihomo 纯编译 | 各 100 次，P95 分别 478.524 / 725.175 / 615.668 ms | 通过 ≤3 s；各自产物 100 次相同 |
| 规模恢复 | 全部 10,707 当前修订解密，10,000 节点 / 500 链 / 200 订阅保留，端到端 27 秒 | 本轮通过 ≤2 h |

[完整分布](baseline/capacity.json)保留 P50/P95/P99、每条路由的样本数与错误率；[资源采样](baseline/resources.jsonl)只移除了终端控制字符，未修改数值。该轮同时验证实际 M2 Schema 15 → M3 Schema 24、不同 ID 的兼容镜像切换、不兼容回退拒绝、独立密钥缺失拒绝和恢复后权限重置。

NFR-13 的 arm64 运行以及 NFR-11 的持续备份调度仍未闭合，T-058 不因容量通过而标记全部完成。
