# T-011 节点生命周期验收

最终源码状态见 `../T-009/m1-validation/quality-source-manifest.json`。CRUD、名称搜索、协议／标签过滤、稳定分页、批量逐项结果、不可变修订、引用、服务端克隆和软删除均已接通真实存储与认证服务。

四项真实 PostgreSQL 节点 HTTP 用例在统一检查全部通过，原始输出见 `../T-009/m1-validation/postgres-tests.jsonl`。专项 race 与 vet 通过，先前执行记录见 `node-verification.txt`。验收涵盖并发相同修订仅一个成功、另一请求 412；克隆新 ID；秘密查看要求重认证与审计；无秘密 DTO；审计失败整笔回滚；200 项批量预条件；持有用户行锁时不出现新增审计的逆向锁等待。

秘密补丁保留／清除／替换均按完整协议校验。新增可选 TLS 指纹 null 和 ALPN 空数组清除的契约、单元与实库回归；REALITY 必填指纹清除仍返回 422。认证变化、REALITY PublicKey／ShortID、停用及删除推进安全 epoch；普通非认证编辑不冒充安全撤销。

真实浏览器覆盖六类表单、保留秘密编辑、TLS 可选值清除、修订冲突草稿、克隆、批量启停和删除确认，见 `../T-048/independent-acceptance.md`。数据库断网曾暴露无响应问题，增加管理请求十秒上下文后，实测安全 503 与恢复通过。

本窗口确认安全状态正确持久化。旧订阅下载阻断随 M2 订阅读取实现验收，当前没有声称完成发布撤销闭环。
