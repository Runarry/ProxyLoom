# G0 技术门槛汇总（限定范围已批准）

## 2026-09-08 人工批准

当前任务用户已明确批准 linux/amd64 Trojan TCP/TLS 原型范围及临时容器测试 CA 方案，关闭 G0 并授权启动 T-004／T-005／T-007、更新 PLAN。详见 [批准记录](../../reviews/2026-09-08-g0-decision.md)。11 项 M0 任务在该限定范围内评审关闭；能力仍为 `unverified`，其他架构、协议、客户端与后续生产门槛不包含在本批准中。以下保留批准前技术记录与当时的待审事项作为历史，不再表示当前 G0 未关闭。

G0 定义（PLAN §3）：同一 IR 三目标正负检查；B 只接受 A；断第一跳无绕行；版本与候选清单冻结。本文件区分**本窗口技术证据**与**仍需人工评审**的事项。不宣布 G0 正式通过，不进入 M1，不把能力标为 `verified`。

## 技术验证结果（linux/amd64，Trojan TCP/TLS）

| 门槛 | 技术结果 | 证据 |
| --- | --- | --- |
| 同一冻结 IR 三目标完整配置 | `Compile` 后三家族原生配置经锁定内核检查并启动 | `verify-compile-exec.mjs`；`verify-live-chain.mjs` |
| 正例 A→B | 三内核经回环入口请求 `/probe` 成功；出口为 B | `matrix.md` forward_chain |
| B 只接受 A | 正确 B 凭证绕过 A → `forbidden_source`；目标成功计数不变 | live bypass；Compose bypass |
| 断第一跳无绕行 | 停 A 后新请求失败；目标无回环直连 | live fail_closed；Compose after-stop-a |
| 方向、复用、整链故障、生命周期 | 见矩阵；原节点不变、标签不串、无 DIRECT/freedom 回退 | live node_reuse / fail_closed / lifecycle |
| 候选版本冻结 | Xray v26.3.27、sing-box v1.14.0、mihomo v1.19.30；`adapter_version=0.1.0-m0-native` | `compat/cores.lock.yaml` |
| 隔离夹具可重建 | 合成凭证、测试 CA、Compose 内部网；不进入开发 Compose | T-053 + 本窗口 Compose 烟测 |

## 未验证或阻塞

2026-09-08 review R1～R4 已修复并补验。新的 [修复报告](remediation-2026-09-08.md) 绑定本轮源码清单，包含 24 个通过场景、51 个通过的 session 清理、9 个零目标流量故障阶段，以及生命周期清理失败必须失败的负例。代码与自动验证问题关闭，仍需下方人工评审。

- arm64 二进制未执行真实链路。
- 本窗口以外的协议／传输／UDP／客户端导入未测。
- T-025 记录的 xanmod 主机 `TestTimeoutAndCancelReapHelper` 未在该主机复测；本窗口在 Docker Debian bookworm 上加强回收并保留原失败记录。
- 能力全部 `unverified`。原生检查通过 + 夹具链路通过 ≠ 发布放行。

## 仍需人工评审

- T-001～T-003、T-023～T-028、T-040、T-053 的 `in_review` 关闭与 done。
- G0 关卡签字：QA 证据、后端确认、TL 批准（PLAN §3）。
- 是否接受“测试 CA 注入临时容器系统信任、不改 Emit”作为 M0 信任方案。
- 是否将 linux/amd64 Trojan TCP/TLS 原型链路作为 G0 范围，明确排除 arm64 与其它组合。
- 不自动进入 M1 或 T-029／T-042。
