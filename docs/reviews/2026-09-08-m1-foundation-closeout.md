# M1 基座工程评审收口（2026-09-08）

基点：`1ff4c094d9d268c8afae82a6987ba216892b2a46`。范围仅 T-004／T-005／T-007。

用户在本任务明确要求实施 T-006／T-008 方案，其中包含基座评审收口。主代理核对既有验收及远端结果，独立只读 explorer 复核资源修订、数据库权限、加密、幂等、API 实际挂载与证据边界，未发现阻断下一窗口的缺陷。此次为 AI 辅助工程评审，不虚构其他人员签字，也不替代正式发布前的独立安全复核。

- [远端运行 34201742618](https://github.com/Runarry/ProxyLoom/actions/runs/34201742618) 对基点提交通过；`checks` 和 `development-smoke` 均 success，包含真实 PostgreSQL 验收。旧文档中的“远端待重跑”仅代表当时状态。
- 复用 `docs/evidence/T-004/validation/`、`ci-linux-fix/` 及 T-005／T-007 的现有源码清单和脱敏证据。没有把本次代码阅读称为重新执行全部验收。
- 数据库运行账号已有明确权限限制；认证必须追加连续迁移，单独授权用户／会话／审计写入。禁止改写历史迁移。
- 现有幂等回执不保存认证 Cookie；认证操作不得将原始会话或 CSRF token 写入幂等缓存。
- 初始化、会话、Origin／CSRF、审计、重置属于 T-006 新工作；既有基座通过不代表这些功能通过。

按用户授权的进入条件，三项限定工程评审关闭，状态转 `done`。G0 批准范围不变，G1 未关闭，内核能力不升级。
