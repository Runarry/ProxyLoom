# M1 输入窗口进入评审

基点：`ae456a293985eb34da78c3bd97fc54ef386bcd82`，授权范围为 ADR-0009。

T-006 的身份应用、身份查询及原有迁移在本窗口没有改写；复用其既有真实 PostgreSQL／HTTPS／CLI 验收和独立接入记录。本窗口节点及任务的真实 HTTP／PostgreSQL 测试继续使用同一身份服务，并验证 Cookie、CSRF、重认证、撤销和数据库失败关闭。新增审计的并发锁顺序问题在新增迁移中修正，不改写已执行身份迁移。

对基点完整 SHA 的远端查询确认 [Engineering checks 34233250087](https://github.com/Runarry/ProxyLoom/actions/runs/34233250087) 已完成且成功。先前以短 SHA 查询返回空列表不能用作没有运行的证据；旧文档的“未提交／未推送”仅保留其当时事实。本窗口新工作树尚未提交或在远端执行。

T-006 限定工程评审收口；记录为 done。该评审由本次 AI 辅助工程审查与既有独立验收组成，不虚构其他人员签字，也不替代 P0 发布前的独立安全复核。

T-008 的本地门槛及基点远端工作流有通过证据，但主分支保护查询仍返回 HTTP 401，无法确认 checks／development-smoke 已设为强制合并条件。因此 T-008 保持 in_review，未修改远端规则。

六项新任务和 T-048 切片的退出判断依各自本窗口证据，不由本进入评审自动批准。G1 未关闭，兼容目录保持 unverified。
